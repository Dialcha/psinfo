#include <stdlib.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    printf("Argumento: %s\n", argv[0]);
    printf("Argumento: %s\n", argv[1]);
    printf("Argumento: %s\n", argv[2]);
    printf("Argumento: %s\n", argv[3]);
    return 0;
}
